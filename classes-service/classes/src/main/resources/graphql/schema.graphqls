type Query {
    classes(teacherId: String!): [Class!]
    batchClasses(teacherIds: [String!]!): [Class!]
}

type Class {
    id: ID!
    subject: String!
    teacherId: String!
    duration: Time! # Custom scalar
}

# Custom scalar for time (e.g., HH:mm format)
scalar Time
